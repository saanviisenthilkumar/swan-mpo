# Manuscript changes from code audit

Sentence-level status only; this file does not replace manuscript prose.

1. **Colon liability — no correction to the scientific equation.** The current Methods explicitly states that Complex I is not penalized in the colorectal panel because it is an intended docking target. This matches the frozen locked model: Colon liability is BBB + neurotoxicity, while Prostate/RCC use BBB + Complex I + neurotoxicity.
2. **Colon single calibrated node — already disclosed.** The current Discussion already states that only mTOR is eligible for the primary colorectal reference-calibrated BestNode and that the binding term therefore represents one calibrated node rather than multi-target support. Do not add a redundant second sentence solely to satisfy the software audit; preserve the existing caveat and ensure it remains in the final manuscript.
3. **Availability of Data and Materials — update at public release.** State that the public tagged release contains the exact canonical 59-compound SwissADME and ProTox inputs, the scoring-required 10,620-row docking extract, canonical 177/531 expected outputs, and a one-command `reproduce-manuscript` regression. Insert the final public GitHub and archive DOI only after logged-out access is verified. A template is provided in `docs/ARCHIVAL_RELEASE_CHECKLIST.md`.
4. **Software implementation — describe the real boundary.** The CLI consumes upstream candidate docking, ADME and toxicity predictions; calculates SWAN safety, ADME, panel liability, calibrated target binding, BestNode, final scores and ranks; and can execute Vina reference-ligand redocking for new targets. It does not automate candidate docking or call SwissADME/ProTox web services.
5. **Expanded SwissADME provenance — direct full export only.** The frozen expanded analysis uses the full SwissADME export with populated Consensus Log P (`expanded_acetogenins_swissadme_CANONICAL_FULL.csv`; SHA-256 `a1b6339695bba431239def8969d56244cecd978e195e5eaf2a3019ca21ec7ab1`). Do not describe any available-model logP averaging/imputation for this frozen analysis.
